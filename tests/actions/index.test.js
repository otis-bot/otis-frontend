import React from 'react';
import { shallow } from 'enzyme';
import { expect } from 'chai';
import actionCreator from '../../src/js/actions/index';

describe('index Action', () => {

  it('Waiting for Testing', () => {
    return true;
  });

});
